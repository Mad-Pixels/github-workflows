name: GitHub Release

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      binaries:
        required: true
        type: string
      working-directory:
        required: true
        type: string

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Parse binary names
        id: parse-binaries
        uses: actions/github-script@v6
        with:
          script: |
            const raw = '${{ inputs.binaries }}';
            if (!raw) {
              throw new Error('No binaries JSON provided');
            }
            const bins = JSON.parse(raw);
            const platforms = [
              { platform: 'linux', arch: 'amd64' },
              { platform: 'linux', arch: 'arm' },
              { platform: 'darwin', arch: 'arm' }
            ];
            const artifacts = bins.flatMap(binary =>
              platforms.map(p => ({
                name: `${binary}_${p.platform}_${p.arch}`,
                binary
              }))
            );
            core.setOutput('artifacts', JSON.stringify(artifacts));

      - name: Download build artifacts
        uses: actions/download-artifact@v3
        with:
          name: binaries
          path: artifacts

      - name: Create Release
        id: create_release
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const version = '${{ inputs.version }}';
            const resp = await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: version,
              release_name: `Release ${version}`,
              draft: false,
              prerelease: false
            });
            core.setOutput('release_id', resp.data.id);
            core.setOutput('upload_url', resp.data.upload_url);

      - name: Upload Release Assets
        run: |
          ARTIFACTS_DIR=artifacts
          UPLOAD_URL=${{ steps.create_release.outputs.upload_url }}
          TOKEN=${{ secrets.GITHUB_TOKEN }}
          ALL_ARTIFACTS='${{ steps.parse-binaries.outputs.artifacts }}'
          for artifact in $(echo "$ALL_ARTIFACTS" | jq -r '.[].name'); do
            binary=$(echo "$ALL_ARTIFACTS" | jq -r --arg name "$artifact" '.[] | select(.name==$name) | .binary')
            file_path="${ARTIFACTS_DIR}/${artifact}/action-${binary}"
            file_name="action-${binary}-${artifact}"
            if [ -f "$file_path" ]; then
              echo "Uploading $file_name"
              curl -X POST "$UPLOAD_URL?name=$file_name" \
                -H "Authorization: Bearer $TOKEN" \
                -H "Content-Type: application/octet-stream" \
                --data-binary "@$file_path"
            else
              echo "Skipping missing file: $file_path"
            fi
          done
